if [[ -d "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.9.2-p136@simple_xlsx_writer" ]] ; then
  \. "$rvm_path/environments/ruby-1.9.2-p136@simple_xlsx_writer"
else
  rvm --create use  "ruby-1.9.2-p136@simple_xlsx_writer"
fi
